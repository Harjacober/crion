ref 14.3 -> implement reallocate without calling malloc, realloc or free
ref 15.2 -> grow the vm stack as needed instead of using a fixed size
ref 19.1 -> use flexible array members technique to store ObjString and its chars
ref 19.2 -> fix inefficient copy of ObjString characters
ref 23.1 -> no need to store duplicate vaeriables in constants table if that variable naame is already in a previous slot.
ref 23.2 -> come up with a more efficient way to store global variables